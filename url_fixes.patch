--- original.html
+++ modified.html
@@ -1324,12 +1324,15 @@
             
             switch(item.type) {
                 case 'url':
+                    const isAllowed = settings.allowlist.some(prefix => item.text.startsWith(prefix));
                     return `
-                        <button class="btn btn-small btn-open" onclick="showUrlModal('${escapeText(item.text)}')">
-                            🌐 Open
+                        <button class="btn btn-small btn-open" onclick="${isAllowed ? `window.open('${escapeText(item.text)}', '_blank')` : `showUrlModal('${escapeText(item.text)}')`}">
+                            🌐 ${isAllowed ? 'Open' : 'Review'}
                         </button>
                         <button class="btn btn-small btn-share" onclick="shareScan('${escapeText(item.text)}')">
                             📤 Share
                         </button>
                     `;
                 case 'wifi':
@@ -165,12 +168,21 @@
             }
 
             saveScan(decodedText);
             showToast('QR code scanned!');
             
+            // Check if it's a URL in allowlist
+            if (decodedText.startsWith('http://') || decodedText.startsWith('https://')) {
+                const isAllowed = settings.allowlist.some(prefix => decodedText.startsWith(prefix));
+                if (isAllowed) {
+                    window.open(decodedText, '_blank');
+                    return; // Don't show modal for allowlisted URLs
+                }
+            }
+            
             // Show appropriate modal based on content type
             showContentModal(decodedText);
         }
 
         function playBeep() {
             try {
@@ -252,12 +264,12 @@
             // Create VirusTotal link (proper base64 encoding)
             const encodedUrl = btoa(url).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
             const virusTotalLink = `https://www.virustotal.com/gui/url/${encodedUrl}`;
             document.getElementById('virusTotalLink').href = virusTotalLink;
             
-            // Only show Open button if in allowlist
+            // Show Open button for all URLs, but with different text
             const openBtn = document.getElementById('modalOpenBtn');
-            openBtn.style.display = isAllowed ? 'block' : 'none';
+            openBtn.textContent = isAllowed ? 'Open' : 'Open Anyway';
+            openBtn.style.display = 'block';
             
             document.getElementById('urlWarning').style.display = isAllowed ? 'none' : 'block';
             
@@ -1024,12 +1036,15 @@
             <div class="action-modal-buttons">
                 <button class="btn btn-open" onclick="openModalUrl()" id="modalOpenBtn">Open</button>
                 <button class="btn btn-copy" onclick="copyModalUrl()">Copy</button>
                 <button class="btn btn-share" onclick="shareModalContent()">Share</button>
                 <button class="btn" onclick="closeUrlModal()" style="background:#444;">Cancel</button>
             </div>
+            <button class="btn btn-share" onclick="shareModalContent()" style="width:100%; margin-top:10px;">
+                📤 Share
+            </button>
 
             <label style="display:flex; align-items:center; gap:10px; margin:15px 0;">
                 <input type="checkbox" id="addToAllowlist">
                 Add to allowlist and open automatically next time
             </label>
